<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
  <body>
    <div th:fragment="cod_support" th:remove="tag">
      <a
        th:if="${sr.codSupported}" 
        class="fas fa-hand-holding-usd fa-2x icon-green"
        th:href="@{'/shipping_rates/cod/'+  ${sr.id} + '/enabled/false' + '?sortField=' + ${sortField} + '&sortDir=' + ${sortDir} + '&keyword=' + ${keyword} + '&page=' + ${currentPage}}"
        title="Disable COD support for this destination"
      ></a>
      <a
        th:unless="${sr.codSupported}"  style="color: black; "
        class="fas fa-hand-holding-usd fa-2x icon-dark"
        th:href="@{'/shipping_rates/cod/' + ${sr.id} + '/enabled/true'+ '?sortField=' + ${sortField} + '&sortDir=' + ${sortDir} + '&keyword=' + ${keyword} + '&page=' + ${currentPage}}"
        title="Enable COD support for this destination"
      ></a>
    </div>
  </body>
</html>
