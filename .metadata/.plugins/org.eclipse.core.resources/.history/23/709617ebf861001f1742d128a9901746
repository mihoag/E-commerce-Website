$(".link-form").on("click", function (e) {
	e.preventDefault();
	var linkDetailURL = $(this).attr("href");
	//console.log(linkDetailURL);
	$("#formModal").modal("show").find(".modal-body").load(linkDetailURL);
});
